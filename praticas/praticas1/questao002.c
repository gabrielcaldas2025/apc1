/*
2. Faça um programa em C que calcule a área de um triãngulo (a = base x altura / 2). Considere base e altura números inteiros maior que 0.template
*/
#include <stdio.h>

int main() {
  int base = 2;
  int altura = 3;

  int area = base * altura / 2;

printf("A area do triangulo e %i/n", area);

  return 0;
}